# AGENTS.md

## 项目原则

- `docs/LEARNING_PATH.md` 是知识范围和资料入口。
- `SUMMARY.md` 是正式目录和正文编写顺序；按目录顺序完整写完一章后，再进入下一章。
- `chapters/` 只保存面向读者的正文，包括待人工审核的初稿；文件进入该目录不表示已经发布。
- `chapters/` 的一级子目录对应一篇，目录名使用 `篇编号-英文主题`；正文文件使用全书连续编号和 `NN｜正文标题.md` 格式。
- `README.md` 不默认创建，也不得承载正式正文；`SUMMARY.md` 必须直接链接具体正文文件。
- `research/` 保存检索过程、来源分歧和版本核验。
- 语雀是人工审核后的发布端；不得绕过审核直接把 AI 初稿标记为已发布。
- 在语雀产生的正文修改必须同步回仓库，仓库 Markdown 保持为唯一内容源。

## 修改规则

1. 新增正文前，先确认主题属于学习线路或项目范围。
2. 正文必须遵守 `docs/CONTENT_SPEC.md`。
3. 新增、删除、重命名或移动正文后更新 `SUMMARY.md`，并检查所有仓库内引用。
4. 涉及版本变化时更新核验日期。
5. 不大段复制第三方资料或代码。
6. 不修改与当前任务无关的文件。
7. 保持最小必要修改。
8. 原理说明优先使用最小实现代码，不默认使用伪代码；示例语言遵守 `docs/CONTENT_SPEC.md`。
9. 版本冲突时以最新正式稳定版本和最新官方资料为主，旧版内容仅在仍有辨识价值时保留并明确标注。
10. 第 00 章是全书 Overview，不设置面试层；第 01 章起的面试速记遵守“背诵提纲、可选高频对比、高频追问”结构。

## 完成任务时输出

- 新增或修改的文件
- 使用的主要来源
- 未解决的问题
- 是否完成事实和代码核验
