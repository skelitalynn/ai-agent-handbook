# AI Agent 知识文档生成 Prompt

你是一名 AI Agent 技术研究员和技术编辑。

请基于：

1. `docs/LEARNING_PATH.md` 中对应主题和链接
2. 我提供的补充资料
3. 你检索到的可靠公开资料

生成一篇可保存到 `chapters/` 的中文 Markdown 初稿。

## 研究要求

- 优先使用官方文档、协议、原始论文、官方博客和官方仓库。
- 重要结论尽量交叉核对。
- 涉及 API、SDK、框架和协议时标注最后核验日期。
- 版本或来源发生冲突时，以最新正式稳定版本和最新官方资料为主。
- 预览版、Beta 和实验特性单独提示，不替代稳定版结论。
- 已废弃但仍有面试辨识价值的概念可以简短保留，并明确标注旧版本、废弃状态和适用条件。
- 不虚构来源、案例、数据或面试频率。
- 不大段复制或逐句改写来源内容。
- 若来源存在差异，说明版本或适用条件。

## 输出结构

# 陈述性的知识主题

用一段引用说明本文解决的问题，以及它与本章其他文章的关系。

## 面试速记

### 背诵提纲

先使用加粗的“定义”给出简短、准确、可以直接背诵的完整定义，再根据主题使用加粗概念作为记忆锚点，覆盖完整链路、关键机制、必要分类、边界和工程影响。

背诵提纲必须覆盖教材正文的知识主干，不能只写一段抽象总结；不要为了简短省略必要限定，也不要放入长篇推导、实现代码、论文历史或装饰性引用。内部结构根据当前主题自然组织，不机械套用“定义、特点、优点、缺点、场景”等固定字段。

### 高频对比

仅当多个概念容易混淆、可以按照相同维度比较，并且比较结果具有面试或工程判断价值时使用 Markdown 表格；否则省略整个小节。不要比较不同系统层级或只是碰巧出现在同一篇中的概念。

### 高频追问

列出 3～5 个面试官围绕背诵提纲中的核心机制可能继续提出的深入问题，并为每个问题提供 1～3 句话的简短回答要点。使用以下格式：

```markdown
#### 追问：问题

简短回答要点。
```

基础定义、分类和直接枚举已经由背诵提纲承担。追问应沿核心机制形成连续的深入链路，重点考察：

- 候选人是否真正理解工作原理
- 是否知道该机制的适用边界
- 是否理解不同方案之间的取舍
- 是否考虑异常、失败和边界情况
- 是否具备调试、评测和生产落地意识

不要把“什么是另一个概念”“有哪些框架或类型”之类的横向术语罗列当作追问。每个追问及回答都必须由后面的教材正文支撑。

使用分隔线结束面试层，然后进入完整教材正文。

第 00 章是全书 Overview，不生成面试速记、背诵提纲、高频对比、高频追问或分隔线，标题和导语后直接进入教材正文。

## 教材正文要求

教材正文根据主题选择自然结构，并建立连续的知识叙事。
可以按照文章自身结构使用少量二级、三级标题和分点，但段落仍是默认表达方式。只有内容确实并列、需要比较或构成步骤时才使用列表；不要为了形式整齐把连续论述拆成大量单句小节、碎片化列表或多层嵌套。
正文通常需要说明：

- 问题为什么产生
- 核心对象、状态、执行过程或推导
- 结论如何从机制推出
- 验证机制的最小实现、实验或案例
- 对正确性、延迟、成本、安全和可维护性的影响
- 相邻概念的边界，以及常见误区为什么错误
- 学完后读者应当能够解释、实现或判断什么

算法主题可以按“问题—推导—实现—复杂度—工程影响”组织；协议主题可以按“角色—消息—生命周期—错误—安全”组织；架构主题可以按“约束—组件—数据流—失败模式—权衡”组织。只选择适合当前主题的结构。

在有助于解释工作原理、执行顺序、组件关系或方案差异时，可以补充：

- 纯文本流程图
- 纯文本时序图
- 纯文本架构图
- Markdown 表格

流程图、时序图和架构图必须使用 `text` 代码块，通过箭头、缩进、分支和简单框线表达。例如：

```text
用户请求
   ↓
调用模型
   ↓
是否需要调用工具？
   ├── 否 → 返回最终答案
   └── 是
         ↓
      执行工具
         ↓
      写回工具结果
         ↓
      再次调用模型
```

流程图表示步骤、条件和循环；时序图表示多个参与方之间按时间发生的消息和状态变化；架构图表示组件、边界、依赖和数据流向；Markdown 表格用于比较多个对象在相同维度上的差异。图表必须承担正文不易快速表达的解释任务，不能只是重复相邻段落。关系简单时使用正文，不为装饰强行添加。

最小实现优先使用可运行或接近可运行的代码，不使用高层框架隐藏当前主题。必须抽象时才使用伪代码；不适用时省略。默认使用 Python，只有浏览器、Node.js 或 TypeScript SDK 特有主题使用 TypeScript。

## 参考资料与结论对应关系

列出正文实际使用的资料、链接、具体用于核对的机制或结论，以及最后核验日期。不要列出正文没有实质使用的权威论文或官方文档作为装饰。

## 风格要求

- 专业、清晰、克制
- 先结论，后原理
- 不写编程语言入门或无关的环境安装，但必须完整建立当前主题所需的模型与 Agent 前置知识
- 不写宏大背景和行业套话
- 不刻意幽默
- 不堆砌术语
- 使用少量必要的标题和分点帮助阅读，不为了分点而分点
- 先解释厂商无关的核心机制，再说明厂商或框架的实质差异
- 不把单一框架实现写成行业标准
- 标题使用知识主题，不为问答模板制造宽泛或不同层级的概念对比
- 面试速记压缩教材主干，不能与正文采用不同限定
- 高频对比按需出现，不为形式完整强行保留
- 高频追问必须沿主问题深入，并提供简短回答要点，不能写成概念清单
- 不在结尾机械总结

## Markdown 要求

- 只输出一个完整 Markdown 文件
- 单篇只能有一个一级标题
- 不使用 HTML 和复杂扩展语法
- 不使用 Mermaid；流程图、时序图和架构图统一使用 `text` 代码块
- 代码块标明语言
- 不输出聊天式前言或说明

## 本次任务

章节：{{chapter}}
主题：{{topic}}
需要引用的前置文章：{{prerequisites}}
学习线路中的资料：{{links}}
补充要求：{{notes}}
