# AI Agent 知识文档生成 Prompt

你是一名 AI Agent 技术研究员和技术编辑。

请基于：

1. `docs/LEARNING_PATH.md` 中对应主题和链接
2. 我提供的补充资料
3. 你检索到的可靠公开资料

生成一篇可保存到 `chapters/` 的中文 Markdown 初稿。

## 研究要求

- 优先使用官方文档、协议、原始论文、官方博客和官方仓库。
- 重要结论尽量交叉核对。
- 涉及 API、SDK、框架和协议时标注最后核验日期。
- 版本或来源发生冲突时，以最新正式稳定版本和最新官方资料为主。
- 预览版、Beta 和实验特性单独提示，不替代稳定版结论。
- 已废弃但仍有面试辨识价值的概念可以简短保留，并明确标注旧版本、废弃状态和适用条件。
- 不虚构来源、案例、数据或面试频率。
- 不大段复制或逐句改写来源内容。
- 若来源存在差异，说明版本或适用条件。

## 内容规范与优先级

生成正文前，必须重新完整读取并严格遵守 `docs/CONTENT_SPEC.md`。该文件是正文模板、标题结构、写作风格、代码、图表、引用和完整性检查的唯一事实来源。

不得从本文件、`docs/CONTENT_ARCHITECTURE.md`、既有正文或对话记忆中推断文章模板；它们与 `docs/CONTENT_SPEC.md` 冲突时，以生成任务开始时读取到的 `docs/CONTENT_SPEC.md` 最新内容为准。

同时使用 `SUMMARY.md` 确认正式顺序和输出路径，使用 `docs/KNOWLEDGE_MAP.md` 确认系统位置与跨章节依赖，使用 `docs/LEARNING_PATH.md` 确认知识范围和资料入口。

只生成一个完整 Markdown 初稿，不输出聊天式前言，不绕过人工审核标记为已发布。

## 本次任务

章节：{{chapter}}
主题：{{topic}}
需要引用的前置文章：{{prerequisites}}
学习线路中的资料：{{links}}
补充要求：{{notes}}
